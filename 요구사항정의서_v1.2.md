# 요구사항정의서 — 사장님신용대출 금리전략 대시보드 (standalone.html) — v1.2

> 본 문서는 `standalone.html` 구현 상태를 반영한 **v1.2 갱신본**입니다.
> v1.1(`요구사항정의서_v1.1.md`, 2026-06-20 기준)을 베이스로, 다음 변경을 반영했습니다.
> 1. **시행 실적 탭 주간 차트 Y축 통일**: 3/31·4/23·5/29 이벤트 탭 전환 시 Y축(억) 상한을 3개 이벤트 공통 고정값으로 변경 — 이벤트 간 거래 규모 성장을 직관적으로 비교 가능
> 데이터(CSV) 입력 항목의 상세 정의는 [`CSV_데이터_가이드.md`](./CSV_데이터_가이드.md)를 함께 참조하세요.

---

## 0. 문서 정보

| 항목 | 내용 |
|---|---|
| 대상 산출물 | `standalone.html` (단일 파일 React 대시보드) |
| 문서 버전 | **v1.2** (작성: 2026-06-24 / v1.1: 2026-06-20 / v1.0: 2026-06-19 / v0.3: 2026-06-16~18 / v0.2: 2026-06-15 / v0.1: 2026-06-13) |
| 사용 목적 | 사장님신용대출 금리전략 변경의 효과 시뮬레이션 + 시행 실적 모니터링 (CEO/ALCO 보고용) |
| 사용 조직 | Corporate여신팀 (금리전략 · 재무) |
| 데이터 상태 | 원가율 6종 · 신규고객 비율(`newCustRatio`) 실측값 반영 완료. 탄력성 계수(`elasticity`)만 가정값 — 실측 예정 |

---

## 1. 시스템 개요

### 1.1 목적
사장님신용대출의 **전략등급별 금리(마진율)를 조정했을 때** 발생하는 다음 효과를 한 화면에서 시뮬레이션하고,
실제 금리 변경 시행 후의 **실적 변화를 추적**하기 위한 대시보드.

1. 신규유입 변화(즉시효과): 신규취급액, 순증액, 대출잔액(전체), 대출건수, 예상 신규고객 유입
2. 손익 변화(균형추정 — 월간 기준): 이자수익, 공헌이익, 영업이익
3. 간접효과: 카드발급고객, 연동 예금평잔, MAU 고객 증가
4. 시행 실적: 과거 3건의 금리 변경 이벤트(3/31, 4/23, 5/29)에 대한 전/후 비교 (주간 영업지표, 월간 수익성, 퍼널 전환율)

### 1.2 기술 스택 / 구현 방식
- **단일 HTML 파일**로 구현 (배포·공유 용이성 우선, 별도 빌드 과정 없음)
- 파일 크기: **84KB** (v1.0 기준 — xlsx 지원 SheetJS 881KB 제거 후)
- `importmap` + esm.sh CDN을 통해 React 18.3.1 / ReactDOM 18.3.1 / Recharts 2.12.7 / lucide-react 0.383.0 로드
- `<script type="text/babel" data-presets="react" data-type="module">` — Babel Standalone으로 브라우저 내 JSX 인라인 트랜스파일
- 차트: Recharts (`BarChart`, `Bar`, `LabelList`, `ResponsiveContainer`, `Tooltip`, `Legend`, `ReferenceLine`, `CartesianGrid`, `XAxis`, `YAxis`)
- 아이콘: lucide-react
- 상태관리: React `useState` / `useMemo`
- **데이터 입력**: 모든 외부 데이터는 **CSV 파일 업로드**를 통해 클라이언트 사이드에서 파싱(FileReader)
- **CSV 다중 인코딩 자동 감지**: UTF-8 BOM → utf-8 / UTF-16 LE BOM → utf-16le / BOM 없음 → EUC-KR 시도 후 UTF-8 폴백

> **CDN 의존성 주의**: 파일 오픈 시 인터넷(esm.sh) 접근이 필요합니다. 행내 PC + 행내 와이파이 환경에서는 정상 작동하며, 오프라인 또는 인터넷 차단 환경에서는 빈 화면이 됩니다. 보고 전날 PDF 백업을 권장합니다.

### 1.3 전체 화면 구성
```
┌─────────────────────────────────────────────────────────────────┐
│ [헤더] Kbank CI 로고 + "금리전략 · 재무" + 대시보드 타이틀          │
│         (우측) 고신용/중저신용 월간 수익성 CSV 업로드 버튼 2개      │
├─────────────────────────────────────────────────────────────────┤
│ [탭 내비게이션] 금리 조정 시뮬레이터 │ 시행 실적                   │
├─────────────────────────────────────────────────────────────────┤
│   [탭1] Simulator  또는  [탭2] ResultPage                         │
├─────────────────────────────────────────────────────────────────┤
│ [푸터] 프로토타입 안내 문구                                        │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 공통 디자인 시스템

### 2.1 색상 테마 (`const C`)
| 토큰 | 값 | 용도 |
|---|---|---|
| `primary` | `#0114A7` | 핵심 강조색(타이틀, "조정 후" 시리즈, 주요 버튼) |
| `secondary` | `#4262FF` | 보조 강조색(eyebrow, 순증액 등 2차 시리즈) |
| `gray` | `#EDF1F7` | 페이지 배경 |
| `border` | `#DCE3EF` | 카드/구분선 테두리 |
| `text` | `#1A2138` | 본문 텍스트 |
| `sub` | `#6B7488` | 보조 텍스트, "현재" 시리즈 |
| `faint` | `#9AA3B5` | 희미한 안내 텍스트, 레거시 잔액 라벨 |
| `cardBg` | `#FFFFFF` | 카드/섹션 배경 |
| `up` | `#16a34a` | 긍정적 변화(증가) |
| `down` | `#dc2626` | 부정적 변화(감소) |
| `barInactive` | `#AEBBD4` | 차트의 "이전/현재" 비교 막대 |
| `gridLine` | `#E3E9F2` | 차트 격자선 |

### 2.2 레이아웃 공통 요소
- **헤더(`S.header`)**: 좌측 로고(58×58px, borderRadius 13) + eyebrow("금리전략 · 재무", 11px, secondary) + title(28px bold, primary), 우측 업로드 버튼
- **탭 내비게이션**: 2개 탭(금리 조정 시뮬레이터 / 시행 실적), 아이콘 + 텍스트
- **카드 그리드(`S.cardGrid`)**: KPI 카드 반응형 그리드
- **차트 카드(`S.chartCard`)**: 흰 배경 카드 + 제목 + `ResponsiveContainer` 차트
- **미니노트(`S.miniNote`)**: 차트/섹션 하단 작은 보조 설명
- **Footer**: "프로토타입 · 탄력성 계수·자금원가율·간접효과 n%는 가정값이며 실측치로 대체 예정"

### 2.3 재사용 컴포넌트
| 컴포넌트 | 역할 |
|---|---|
| `KpiCard` | 라벨/값/단위 + delta% 증감 표시. `animDelay`로 순차 등장 애니메이션 |
| `IndirectCard` | 간접효과 카드: 아이콘+라벨+값 + 사용자 직접 입력 전환율(n%) 컨트롤 |

### 2.4 수치 표시 포맷 규칙
| 지표 유형 | 표시 형식 | 적용 위치 |
|---|---|---|
| 물량 지표 (억원) | 소수점 1자리 (예: `48.3`) | Simulator 신규유입 카드, 결과 테이블, ResultPage 주간 KPI, 주간 차트 |
| 대출 잔액 (총합) | 총합 소수점 1자리, 하단 전략/레거시 구분 표기 | Simulator "신규유입 변화" 잔액 카드 |
| 수익성 지표 (백만원/월) | 정수 + 천단위 콤마 | Simulator 추정손익 카드, ResultPage 수익성 KPI, 통합 월간 차트 |
| 수익성 지표 (억원/연간) | 소수점 1자리 | Simulator 결과 테이블 |
| 건수/인원 (건/명) | 정수 + 천단위 콤마 | 대출건수, 신규고객, 카드발급/MAU 고객 |
| 비율 (%) | 소수점 1자리 | 결과 테이블 이탈율, 퍼널 차트, KPI 카드 delta |
| 간접효과 — 연동 예금평잔 | 정수 + 천단위 콤마, 단위: 백만원 | Simulator 간접효과 카드 |

---

## 3. 화면별 요구사항

### 3.1 탭 1 — 금리 조정 시뮬레이터 (`Simulator`)

#### 3.1.1 목적
전략등급(1~7등급)별로 금리(마진율)를 ±50bp 범위에서 조정했을 때, 신규유입·이탈률·잔액·수익성·신규고객 유입에 미치는 영향을 "현재" 대비 "조정 후" 값으로 즉시 비교한다.

> **등급 범위 정책**: 신규 승인 대상은 **1~7등급**만. 8~10등급 레거시 잔액(165.2억)은 총 잔액 카드에만 구분 표기.

#### 3.1.2 화면 구성 (상→하 순서)

##### (1) 헤더 영역
- Gauge 아이콘 + "전략등급 금리 조정 시뮬레이터"
- 버튼 3종: **최적금리 찾기(참고)** / **초기화** / **기준값 CSV** (Upload)

##### (2) 가정값 안내 박스
- 문구: "탄력성 계수 및 원가율 6종은 등급별 **가정값**이며 실측치 확보 시 기준값 CSV로 교체하세요."
- 핵심 산식:
  - **공헌이익 = 이자수익 − 자금원가율 + 법적비용 + 계약신용원가** _(비용율 CSV 음수(−) 입력)_
  - **영업이익 = 공헌이익 − 계약업무원가 − 신용원가gap − 업무원가gap + 비이자이익** _(비용율 CSV 음수(−) 입력)_
- CSV 헤더 스펙: `grade,balance,count,newWk,netWk,churn,margin,`**`legalCostRate,contractCreditRate,contractOpRate,creditGapRate,opGapRate,nonIntRate`**`,elasticity,`**`newCustRatio`**
- 현재 적용 파일명 표시 (`현재: {baseFile}`)
- **자금원가율(cof) 입력**: 기본값 **2.8%**, 즉시 반영

##### (3) 등급별 조정 슬라이더
- 1~7등급, 각 행: 슬라이더(`min=-50, max=50, step=5bp`) + 조정값 표시 + `ε {elasticity}` 표시

##### (4) "신규유입 변화" 섹션 — 즉시효과

| 라벨 | 단위 | 산출 | 비고 |
|---|---|---|---|
| 신규취급액 (주) | 억 | `newWkOld → newWkNew` (소수점 1자리) | — |
| 순증액 (주) | 억 | `netWkOld → netWkNew` (소수점 1자리) | — |
| **대출 잔액 (총합)** | 억 | `(balOld+165.2) → (balNew+165.2)` (소수점 1자리) | 전략 1~7등급 + 레거시 8~10등급 구분 2줄 표기 |
| 대출 건수 (주간) | 건 | `cntOld → cntNew` (정수, 콤마) | — |
| **예상 신규고객 유입(주간)** | 명 | `custGainOld → custGain` (정수, 콤마) | 하단에 "3개월 평균 당행 최초 등록고객 비율 반영" 표시 |

- `custGainOld = Σ(cntOld × newCustRatio/100)` / `custGain = Σ(cntNew × newCustRatio/100)` (절대 수치, before/after 비교)
- `newCustRatio`: 해당 등급 실행 고객 중 당행 최초 등록 고객 비율(3개월 평균 **실측값**)

##### (5) "추정 손익" 섹션 — 균형추정(월간 기준)

| 라벨 | 단위 | 산출 |
|---|---|---|
| 이자수익 (월) | 백만원 | `toMonthlyMM(intOld) → toMonthlyMM(intNew)` |
| 공헌이익 (월) | 백만원 | `toMonthlyMM(cmOld) → toMonthlyMM(cmNew)` |
| 영업이익 (월) | 백만원 | `toMonthlyMM(opOld) → toMonthlyMM(opNew)` |

- 미니노트: "추정손익은 변경된 조정금리가 지속되어 포트폴리오 내 모든 고객이 새 조정금리를 적용받는 균형점에 도달했을 때의 추정치입니다"

##### (6) 등급별 상세 결과 테이블
- 1~7등급, 가로 스크롤
- 컬럼: 등급 / 조정(bp) / 이탈율(%) / 건수(주) / 취급액(주,억) / 순증액(주,억) / 잔액(억) / 이자수익(억) / 공헌이익(억) / 영업이익(억)

##### (7) 요약 차트 ① — 주간 물량
- BarChart, 3개 카테고리: 신규실행액 / 대출건수 / 순증액 (1~7등급 기준)
- 막대 2계열: `curr`("현재", barInactive) / `adj`("조정 후", primary)
- `barSize=40`, `barCategoryGap="20%"`, 높이 280px, 막대 상단 LabelList

##### (8) 요약 차트 ② — 월간 수익성
- BarChart, 2개 카테고리: 이자수익 / 공헌이익 (백만원/월)
- 막대 2계열: `curr` / `adj`, 높이 280px

##### (9) 간접효과 섹션
- **gainBanner**: "조정 후 주간 신규고객 **{N}명** 기준" (`custGain` 절대 수치, primary 색)
- IndirectCard 3종:

| 카드 | 분모 | 산출 |
|---|---|---|
| 카드발급고객 증가 | `custGain` (절대) | `custGain × (cardRate/100)` 명, 기본 cardRate=35% |
| 연동 예금평잔 (3개월) | `custGain` (절대) | `custGain × depositRate / 100` 백만원, 기본 depositRate=120만원 |
| MAU 고객 증가 | `custGain` (절대) | `custGain × (mauRate/100)` 명, 기본 mauRate=60% |

- 미니노트: "**\* n 값은 사장님 대출 보유고객의 3개월 평균 실측 값이 입력되어 있으며, 수기 조정 가능함.**"

---

### 3.2 탭 2 — 시행 실적 (`ResultPage`)

#### 3.2.1 목적
과거 3건의 금리 변경 이벤트(3/31, 4/23, 5/29)에 대해 전/후의 주간 영업지표, 월간 수익성, 퍼널 전환율 변화를 CSV 기준으로 확인.

> **수익성 지표(공헌이익/영업이익)는 simbase 원가율과 무관합니다.** `template_3_monthly_*.csv`에 입력된 실측값(원천 시스템 산출 완료값)을 그대로 집계·표시합니다.

#### 3.2.2 이벤트 정의
| ID | 라벨 | 기준 주(refWeek) | 전(before) 월 | 후(after) 월 |
|---|---|---|---|---|
| `ev331` | 3/31 본부조정금리변경 | 4월 1주 | 1월·2월·3월 | 4월 |
| `ev423` | 4/23 본부조정금리변경 | 5월 1주 | 3월·4월 | 5월 |
| `ev529` | 5/29 토스/현카 우대금리 도입 | 6월 1주 | 4월·5월 | 6월 |

#### 3.2.3 화면 구성 (상→하)

##### (1) 안내 배너: 이벤트별 탭 선택 → 해당 변경 전/후 지표 표시, 수익성은 익월 4일 집계 기준

##### (2) 이벤트 탭 선택 (3개)

##### (3) 이벤트별 업로드: 주간 실적 CSV / 퍼널 CSV

##### (4) 주간 영업 지표 KPI (5종)
`newLoan` / `netLoan` / `balance` / `count` / `newCust` — before 평균 대비 after 평균 delta%

##### (5) 주간 차트: 신규실행액·순증액 추이, ReferenceLine(금리변경일)

> **v1.2 변경 — Y축(억) 3개 이벤트 공통 고정**: 기존에는 활성 이벤트 탭의 데이터에만 맞춰 Y축이 자동 스케일링되어, 탭을 넘길 때마다 축 단위가 달라져 "이벤트가 진행될수록 거래 규모가 커진다"는 성장 추이가 직관적으로 보이지 않았다. 이를 개선해 **3개 이벤트(`ev331`/`ev423`/`ev529`) 전체 데이터를 기준으로 산출한 공통 Y축 상한**을 모든 탭에 동일하게 적용한다.
> - 산식: `weeklyAxisMax = ceil(max(전체 이벤트 newLoan·netLoan 값) × 1.15 / 10) × 10` (§4.7 참조)
> - 효과: 탭 전환 시 막대 높이 자체로 이벤트 간 절대 규모 성장이 시각적으로 드러남(더 극적인 비교)
> - 트레이드오프: 거래 규모가 상대적으로 작거나 변동폭이 좁은 이벤트(예: 1차 변경 직후)는 막대가 작게 보일 수 있음 → 막대 상단 LabelList 숫자 라벨로 전/후 세부 변화를 보완

##### (6) 수익성 지표
- (6-1) 고신용자: `template_3_monthly_high.csv` 기준, `interest`/`contribution`/`operatingProfit` × 100 (백만원)
- (6-2) 중저신용자: `template_3_monthly_low.csv` 기준
- (6-3) 통합 월간 추이 차트: 고신용+중저신용 합산, 1월~6월 막대 3계열

##### (7) 퍼널 전환율: 플랫폼→당행 유입률 / 가심사승인→실행 전환율 (등급별 before/after 바차트)

---

## 4. 계산 로직 정의

### 4.1 공통 유틸리티
| 함수 | 정의 |
|---|---|
| `avg(rows, key)` | 배열 평균 (빈 배열→0) |
| `sumBy(rows, key)` | 배열 합계 |
| `toMonthlyMM(v)` | `v × 100 / 12` — 연간(억) → 월간(백만원) |

### 4.2 `computeRows(adjMap, base, cofVal)` — 등급별 시뮬레이션

> **부호 규칙**: `legalCostRate`, `contractCreditRate`, `contractOpRate`, `creditGapRate`, `opGapRate` 5종은 CSV에서 음수(−) 입력. `nonIntRate`·`newCustRatio`는 양수(+) 입력.

```
churnNew    = max(0, churn + (bp/10) × elasticity)
retainRatio = (100 − churnNew) / (100 − churn)
balNew      = balance × retainRatio

marginNew   = margin + bp/100
intOld      = balance × margin / 100
intNew      = balNew  × marginNew / 100

// 음수 입력값 → + 처리로 차감 효과 동일
cmRateOld   = margin    − cof + legalCostRate + contractCreditRate
cmRateNew   = marginNew − cof + legalCostRate + contractCreditRate
cmOld/New   = balance(balNew) × cmRate / 100

// 음수 입력값 → − 처리로 양수 비용 전환
opAdjRate   = −contractOpRate − creditGapRate − opGapRate − nonIntRate
opOld/New   = cmOld/New − balance(balNew) × opAdjRate / 100

cntNew = count × retainRatio  /  newWkNew = newWk × retainRatio  /  netWkNew = netWk × retainRatio
newCustRatio: 해당 등급 실측 신규고객 비율 — computeRows 반환값에 포함
```

### 4.3 `runSimulation(adjMap, base, cofVal, indirectParams)`

```
// 신규고객 유입 수 (절대값)
custGainOld = Σ (r.cntOld × r.newCustRatio / 100)   // 현재 주간 신규고객 수
custGain    = Σ (r.cntNew × r.newCustRatio / 100)   // 조정 후 주간 신규고객 수

// 간접효과 — custGain(조정 후 절대 수치) 기준
cardEffect    = custGain × (cardRate / 100)
depositEffect = custGain × depositRate / 10000      // 억; 표시 시 ×100 → 백만원
mauEffect     = custGain × (mauRate / 100)

반환: { rows, tot, indirect: { custGainOld, custGain, cardEffect, depositEffect, mauEffect } }
```

### 4.4 `findOptimalAdj(base, cofVal, { churnCap=15, range=50, step=5 })`
- 등급별 grid search: 공헌이익 최대화 bp, 이탈률 증가 ≤ 15%p 제약
- cm 산식: `margin + bp/100 − cof + legalCostRate + contractCreditRate`

### 4.5 ResultPage 집계
- 주간 KPI: `avg(before/after, key)` → delta%
- 월간 KPI: `avg(filtered by beforeMonths/afterMonths, key) × 100` (억→백만원)
- 통합 차트: 고신용 + 중저신용 합산 × 100

### 4.6 `LEGACY_BALANCE` 상수
```js
const LEGACY_BALANCE = 165.2; // 8~10등급 레거시 잔액(억) — 조정 범위 외
```
- 적용: Simulator "대출 잔액 (총합)" 카드 단 한 곳
- 시뮬레이션 반영 안 됨 (취급액/수익성 계산은 1~7등급만)
- 변경 시 HTML 내 상수 직접 수정

### 4.7 `weeklyAxisMax` — 주간 차트 공통 Y축 상한 (ResultPage, v1.2 신설)

```
vals          = 모든 이벤트(weeklyByEvent: ev331+ev423+ev529)의 newLoan, netLoan 값 전체
weeklyAxisMax = ceil( max(vals) × 1.15 / 10 ) × 10
```

- **계산 범위**: 현재 활성 탭이 아니라 **3개 이벤트 전체** 데이터를 합쳐서 산출 → 탭을 전환해도 Y축이 변하지 않고 고정됨.
- **`× 1.15` 헤드룸**: 막대 최댓값이 축 상한에 딱 맞으면 상단 LabelList 숫자 라벨이 잘릴 수 있어 15% 여유 공간을 둔다.
- **`useMemo([weeklyByEvent])`**: 이벤트별 CSV를 새로 업로드할 때마다 `weeklyByEvent` 변경에 반응하여 자동 재계산.
- 적용 위치: ResultPage 주간 차트(§3.2.3 (5))의 `<YAxis domain={[0, weeklyAxisMax]} />`.

---

## 5. CSV 데이터 입력 명세

> 컬럼별 상세 정의: [`CSV_데이터_가이드.md`](./CSV_데이터_가이드.md)

> **비용율 부호 규칙**: `legalCostRate`, `contractCreditRate`, `contractOpRate`, `creditGapRate`, `opGapRate` → **음수(−)** 입력. `nonIntRate`, `newCustRatio` → **양수(+)** 입력.

| CSV 파일 | 업로드 위치 | 적용 범위 | 파서 |
|---|---|---|---|
| `template_2_simbase.csv` | 시뮬레이터 탭 "기준값 CSV" | 1~7등급 시뮬레이션 기준값 | `parseSimBase` |
| `template_1_weekly_ev331/423/529.csv` | 시행 실적 탭 이벤트별 "주간 실적 CSV" | 해당 이벤트 주간 영업지표 (3개 이벤트 합산값이 §4.7 Y축 상한 산출에도 사용됨) | `parseWeekly` |
| `template_3_monthly_high.csv` | 헤더 "고신용 수익성 CSV" | 고신용 1~6월 (실측 완제 수치) | `parseMonthly` |
| `template_3_monthly_low.csv` | 헤더 "중저신용 수익성 CSV" | 중저신용 1~6월 (실측 완제 수치) | `parseMonthly` |
| `template_4_funnel_ev331/423/529.csv` | 시행 실적 탭 이벤트별 "퍼널 CSV" | 해당 이벤트 퍼널 전환율 | `parseFunnelWide` |

**`template_2_simbase.csv` 컬럼 현황**:
| 컬럼 | 데이터 상태 |
|---|---|
| 원가율 6종 (`legalCostRate` 등) | ✅ 실측값 |
| `newCustRatio` (당행 최초 등록고객 비율) | ✅ 실측값 (3개월 평균) |
| `elasticity` (금리 탄력성 계수) | ⚠️ 가정값 — 과거 이벤트 역산 예정 |
| `margin`, `balance`, `count` 등 영업지표 | ✅ 실측값 |

---

## 6. 비기능 요구사항

| 구분 | 내용 |
|---|---|
| 배포 형태 | 단일 HTML 파일 — 서버/빌드 없이 브라우저 직접 실행 |
| 파일 크기 | **84KB** |
| 데이터 영속성 | 없음 — 새로고침 시 데모 데이터로 초기화 |
| 데모 데이터 | `DEMO_WEEKLY`, `DEMO_MONTHLY_HIGH/LOW`, `DEMO_SIM_BASE`, `DEMO_FUNNEL` 내장 |
| CSV 인코딩 | UTF-8 BOM / UTF-16 LE BOM / EUC-KR 자동 감지 |
| 네트워크 의존성 | esm.sh CDN 인터넷 접근 필요. 행내 PC + 행내 와이파이 환경에서 정상 작동. 오프라인 불가. |
| 반응형 | `repeat(auto-fit, minmax(...))` 기반 그리드 |
| 언어/단위 | 한국어 전용. 억원(물량) / 백만원(월간 수익성·예금평잔) |

---

## 7. 미결정 / 추후 협의 항목 (Open Items)

1. **탄력성 계수(`elasticity`) 실측화**: 현재 가정값. 과거 금리 변경 이벤트 3개(3/31, 4/23, 5/29) 전후 이탈률 변화 데이터를 활용해 등급별 역산 예정 (시행 실적 탭 데이터 활용 가능). → **다음 주 진행 예정**
2. **자금원가율(cof) 자동 연동**: 현재 직접 입력(기본값 2.8%). 자동화 방식 전환 검토.
3. **간접효과 전환율(n%) 검증**: 현재 3개월 평균 실측값 반영. 정기 갱신 주기 및 방식 정립 필요.
4. **고신용/중저신용 분류 기준 문서화**: 신용평가 등급 컷오프 기준 명확화 필요.
5. **이벤트(EVENTS) 확장성**: 신규 금리 변경 이벤트 추가 시 `EVENTS` 배열 + CSV 3종 + `beforeMonths`/`afterMonths` 동시 갱신 필요. **신규 이벤트 추가 시 §4.7 `weeklyAxisMax`도 자동으로 신규 이벤트 데이터를 포함해 재계산됨(별도 수정 불필요).**
6. **연동 예금평잔 단위 적절성**: 백만원 표시 자릿수 실데이터 기준 재검토.
7. **`LEGACY_BALANCE` 갱신 프로세스**: 하드코딩(165.2억). 변동 시 HTML 직접 수정 필요. 8~10등급 잔액 소진 시 상수 제거 예정.
8. **오프라인 대응**: CDN 의존 구조상 오프라인 불가. 중요 보고 전날 PDF 백업 권장.
9. **주간 차트 Y축 헤드룸 비율(1.15) 검증**: 실데이터 업로드 후 라벨 잘림/여백 과다 여부 육안 확인 필요. 필요 시 계수 조정.

---

## 8. 변경 이력 (요약)

| 일자 | 버전 | 변경 내용 |
|---|---|---|
| 2026-06-12 | — | Kbank CI 로고 추가, 수익성 지표 월/백만원 단위 통일(ROA 제거), 요약 차트 주간/월간 분리, backup0612 생성 |
| 2026-06-13 | v0.1 | 로고 58px 확대, eyebrow "금리전략 · 재무", 차트 barSize=40/barCategoryGap=20% 통일, LabelList 전체 적용, simbase `interestRate`→`margin` 수정, `CSV_데이터_가이드.md` / `요구사항정의서_v0.1` 작성 |
| 2026-06-15 | v0.2 | 물량 지표 소수점 1자리 표시, 통합 월간 차트 Y축 수정, 연동예금평잔 억→백만원, `요구사항정의서_v0.2` 작성 |
| 2026-06-16 | v0.3 | `LEGACY_BALANCE=140` 추가, 잔액 카드 1~10등급 합산 + 전략/레거시 구분 표기, `요구사항정의서_v0.3` 작성 |
| 2026-06-18 | v0.3수정 | `LEGACY_BALANCE` 165.2억 업데이트, ev529 5월5주 행 추가, balance 연속성 보완 |
| 2026-06-19 | v1.0 | ① xlsx→CSV 전환(SheetJS 제거, 84KB) ② CSV 다중 인코딩 자동감지 ③ 원가율 5종 CSV 음수 입력 방식 확정 + 산식 부호 수정 + 데모값/템플릿 갱신 ④ `요구사항정의서_v1.0` / `CSV_데이터_가이드` 갱신 |
| 2026-06-20 | v1.1 | ① cof 기본값 1.7→2.8% ② `newCustRatio` 컬럼 신설 — 등급별 당행 최초 등록고객 비율(실측값) ③ 신규고객 카드: "예상 신규고객 유입(주간)" 절대 before/after 비교로 개편 ④ 간접효과 분모: 증가분 → 조정 후 절대 신규고객 수 ⑤ gainBanner/miniNote 문구 변경 ⑥ ADW 연동 안내 배너 삭제 ⑦ `standalone(backup0620).html` 백업 생성 |
| 2026-06-24 | **v1.2** | 시행 실적 탭 주간 차트(신규실행액·순증액) **Y축(억) 3개 이벤트 공통 고정** — `weeklyAxisMax = ceil(max(전체 이벤트 newLoan·netLoan) × 1.15 / 10) × 10` 산식으로 탭 전환 시에도 축이 변하지 않도록 변경(§3.2.3(5), §4.7 신설). 이벤트 진행에 따른 거래 규모 성장을 시각적으로 더 직관적으로 비교할 수 있도록 개선. |
