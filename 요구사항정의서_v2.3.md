# 요구사항정의서 — 사장님신용대출 금리전략 대시보드 (standalone.html) — v2.3

> 본 문서는 `standalone.html` 구현 상태를 반영한 **v2.3 갱신본**입니다.
> v2.2(`요구사항정의서_v2.2.md`, 2026-06-30 기준)를 베이스로, 다음 변경을 반영했습니다.
> 1. **탭 버튼 명칭 변경**: "시행 실적" → **"성과 모니터링"**
> 2. **수익성 지표 섹션 안분 추정 안내 배너 추가**: 고신용/중저신용 수익성 지표가 현재 고객 비율 기반 안분 추정치임을 명시 (실데이터 반영 예정: 2026년 8월말)
> 3. **성과 모니터링 탭 — 이벤트별 금리 조정 내역 칩 추가**: 탭 선택 시 해당 이벤트의 등급별 bp 조정 내역을 칩으로 표시 (하드코딩)
> 4. **ev529 이벤트 정의 수정**: 라벨 "5/29 토스/현카 우대금리 도입" → **"5/29 토스 우대금리 도입"**, bp 내역 "전 등급 −3bp" → **"토스 플랫폼 유입고객 전 등급 −10bp"** (가중평균 3bp가 아닌 실제 적용 할인폭으로 표기)
> 데이터(CSV) 입력 항목의 상세 정의는 [`CSV_데이터_가이드.md`](./CSV_데이터_가이드.md)를 함께 참조하세요.

---

## 0. 문서 정보

| 항목 | 내용 |
|---|---|
| 대상 산출물 | `standalone.html` (단일 파일 React 대시보드) |
| 문서 버전 | **v2.3** (작성: 2026-07-02 / v2.2: 2026-06-30 / v2.1: 2026-06-26 / v2.0: 2026-06-25 / v1.2: 2026-06-24 / v1.1: 2026-06-20 / v1.0: 2026-06-19 / v0.3: 2026-06-16~18 / v0.2: 2026-06-15 / v0.1: 2026-06-13) |
| 사용 목적 | 사장님신용대출 금리전략 변경의 효과 시뮬레이션 + 시행 실적 모니터링 (CEO/ALCO 보고용) |
| 사용 조직 | Corporate여신팀 (금리전략 · 재무) |
| 데이터 상태 | 원가율 6종 · 신규고객 비율(`newCustRatio`) · 탄력성 계수(1~4등급, 6월4주 확정치 기준 1.65/0.93/0.36/0.74) 실측값 반영 완료. 5~7등급 elasticity는 표본 부족으로 보류 |

---

## 1. 시스템 개요

### 1.1 목적
사장님신용대출의 **전략등급별 금리(마진율)를 조정했을 때** 발생하는 다음 효과를 한 화면에서 시뮬레이션하고,
실제 금리 변경 시행 후의 **실적 변화를 추적**하기 위한 대시보드.

1. 신규유입 변화(즉시효과) + **+3개월 예상 대출잔액**: 신규취급액, 순증액, 대출잔액, 대출건수, 예상 신규고객 유입
2. 손익 변화(+3개월 후 예상 — 월간 기준): 이자수익, 공헌이익, 영업이익
3. 간접효과(주간): 신규 카드발급 고객 증가, 수신평잔 증가효과, MAU 고객증가
4. 성과 모니터링: 과거 3건의 금리 변경 이벤트(3/31, 4/23, 5/29)에 대한 전/후 비교 (주간 영업지표, 월간 수익성, 퍼널 전환율)

### 1.2 기술 스택 / 구현 방식
- **단일 HTML 파일**로 구현 (배포·공유 용이성 우선, 별도 빌드 과정 없음)
- 파일 크기: **약 90KB**
- `importmap` + esm.sh CDN을 통해 React 18.3.1 / ReactDOM 18.3.1 / Recharts 2.12.7 / lucide-react 0.383.0 로드
- `<script type="text/babel" data-presets="react" data-type="module">` — Babel Standalone으로 브라우저 내 JSX 인라인 트랜스파일
- 차트: Recharts (`BarChart`, `Bar`, `LabelList`, `ResponsiveContainer`, `Tooltip`, `Legend`, `ReferenceLine`, `CartesianGrid`, `XAxis`, `YAxis`)
- 아이콘: lucide-react
- 상태관리: React `useState` / `useMemo`
- **데이터 입력**: 모든 외부 데이터는 **CSV 파일 업로드**를 통해 클라이언트 사이드에서 파싱(FileReader)
- **CSV 다중 인코딩 자동 감지**: UTF-8 BOM → utf-8 / UTF-16 LE BOM → utf-16le / BOM 없음 → EUC-KR 시도 후 UTF-8 폴백

> **CDN 의존성 주의**: 파일 오픈 시 인터넷(esm.sh) 접근이 필요합니다. 행내 PC + 행내 와이파이 환경에서는 정상 작동하며, 오프라인 또는 인터넷 차단 환경에서는 빈 화면이 됩니다. 보고 전날 PDF 백업을 권장합니다.

### 1.3 전체 화면 구성
```
┌─────────────────────────────────────────────────────────────────┐
│ [헤더] Kbank CI 로고 + "금리전략 · 재무" + 대시보드 타이틀          │
│         (우측) 고신용/중저신용 월간 수익성 CSV 업로드 버튼 2개      │
├─────────────────────────────────────────────────────────────────┤
│ [탭 내비게이션] 금리 조정 시뮬레이터 │ 성과 모니터링               │
├─────────────────────────────────────────────────────────────────┤
│   [탭1] Simulator  또는  [탭2] ResultPage                         │
└─────────────────────────────────────────────────────────────────┘
```

---

## 2. 공통 디자인 시스템

### 2.1 색상 테마 (`const C`)
| 토큰 | 값 | 용도 |
|---|---|---|
| `primary` | `#0114A7` | 핵심 강조색(타이틀, "조정 후" 시리즈, 주요 버튼) |
| `secondary` | `#4262FF` | 보조 강조색(eyebrow, 순증액 등 2차 시리즈) |
| `gray` | `#EDF1F7` | 페이지 배경 |
| `border` | `#DCE3EF` | 카드/구분선 테두리, 슬라이더 보조 눈금(5bp 단위) |
| `text` | `#1A2138` | 본문 텍스트 |
| `sub` | `#6B7488` | 보조 텍스트, "현재" 시리즈 |
| `faint` | `#9AA3B5` | 희미한 안내 텍스트, 레거시 잔액 라벨, 슬라이더 주 눈금(10bp 단위) |
| `cardBg` | `#FFFFFF` | 카드/섹션 배경 |
| `up` | `#16a34a` | 긍정적 변화(증가) |
| `down` | `#dc2626` | 부정적 변화(감소) |
| `barInactive` | `#AEBBD4` | 차트의 "이전/현재" 비교 막대 |
| `gridLine` | `#E3E9F2` | 차트 격자선 |

### 2.2 레이아웃 공통 요소
- **헤더(`S.header`)**: 좌측 로고(58×58px, borderRadius 13) + eyebrow("금리전략 · 재무", 11px, secondary) + title(28px bold, primary), 우측 업로드 버튼
- **탭 내비게이션**: 2개 탭(금리 조정 시뮬레이터 / **성과 모니터링**), 아이콘 + 텍스트
- **카드 그리드(`S.cardGrid`)**: KPI 카드 반응형 그리드
- **차트 카드(`S.chartCard`)**: 흰 배경 카드 + 제목 + `ResponsiveContainer` 차트
- **미니노트(`S.miniNote`)**: 차트/섹션 하단 작은 보조 설명

### 2.3 재사용 컴포넌트
| 컴포넌트 | 역할 |
|---|---|
| `KpiCard` | 라벨/값/단위 + delta% 증감 표시. `animDelay`로 순차 등장 애니메이션 |
| `IndirectCard` | 간접효과 카드: 아이콘+라벨+값 + 사용자 직접 입력 전환율(n%) 컨트롤 |

### 2.4 수치 표시 포맷 규칙
| 지표 유형 | 표시 형식 | 적용 위치 |
|---|---|---|
| 물량 지표 (억원) | 소수점 1자리 (예: `48.3`) | Simulator 신규유입 카드, 결과 테이블, ResultPage 주간 KPI, 주간 차트 |
| **+3M 예상 대출잔액 (총합)** | 총합 소수점 1자리, 하단 전략/레거시 구분 표기 | Simulator "신규유입 변화" 잔액 카드 |
| 수익성 지표 (백만원/월, +3M 후 예상) | 정수 + 천단위 콤마 | Simulator 추정손익 카드, ResultPage 수익성 KPI, 통합 월간 차트 |
| 수익성 지표 (억원/연간, +3M 후 예상) | 소수점 1자리 | Simulator 결과 테이블 |
| 건수/인원 (건/명) | 정수 + 천단위 콤마 | 대출건수, 신규고객, 카드발급/MAU 고객 |
| 비율 (%) | 소수점 1자리 | 결과 테이블 이탈율, 퍼널 차트, KPI 카드 delta |
| 간접효과 — 수신평잔 증가효과 (주간) | 정수 + 천단위 콤마, 단위: 백만원 | Simulator 간접효과 카드 |

---

## 3. 화면별 요구사항

### 3.1 탭 1 — 금리 조정 시뮬레이터 (`Simulator`)

#### 3.1.1 목적
전략등급(1~7등급)별로 금리(마진율)를 ±50bp 범위에서 조정했을 때, 신규유입·이탈률·잔액·수익성·신규고객 유입에 미치는 영향을 "현재" 대비 "조정 후" 값으로 즉시 비교한다.

> **등급 범위 정책**: 신규 승인 대상은 **1~7등급**만. 8~10등급 레거시 잔액(165.2억)은 총 잔액 카드에만 구분 표기.

#### 3.1.2 화면 구성 (상→하 순서)

##### (1) 헤더 영역
- Gauge 아이콘 + "전략등급 금리 조정 시뮬레이터"
- 버튼 4종: **균형성장(공헌이익 위주)** / **균형성장(자산성장 위주)** / **초기화** / **기준값 CSV** (Upload)

##### (2) 안내 박스
- 아이콘: `Info`(참고용 안내)
- 문구: "탄력성 계수: 등급별 **실측 다중회귀분석** 기반(2026-06)"
- 핵심 산식:
  - **공헌이익 = 이자수익 − 자금원가율 + 법적비용 + 계약신용원가** _(비용율 CSV 음수(−) 입력)_
  - **영업이익 = 공헌이익 − 계약업무원가 − 신용원가gap − 업무원가gap + 비이자이익** _(비용율 CSV 음수(−) 입력)_
- CSV 헤더 스펙: `grade,balance,count,newWk,netWk,churn,margin,`**`legalCostRate,contractCreditRate,contractOpRate,creditGapRate,opGapRate,nonIntRate`**`,elasticity,`**`newCustRatio`**
- 현재 적용 파일명 표시 (`현재: {baseFile}`)
- **자금원가율(cof) 입력**: 기본값 **2.8%**, 즉시 반영

##### (3) 등급별 조정 슬라이더 _(v2.2 변경)_
- 1~7등급, 각 행: 슬라이더(`min=-50, max=50, step=5bp`) + 조정값 표시 + `ε {elasticity}` 표시
- 슬라이더는 **네이티브 `<input type="range">`를 그대로 사용**(파란색 채움 효과 유지를 위해 `accentColor` 외 별도 외형 재정의 없음)
- 슬라이더 트랙 바로 아래에 **5bp 단위 눈금**을 별도 레이어로 표시: **10bp 배수**(−50,−40,...,40,50)는 `C.faint` 색으로 굵고 길게, 그 사이 **5bp 단위**(−45,−35,...,35,45)는 `C.border` 색으로 얇고 짧게 — 슬라이더 자체 렌더링과는 분리된 장식 요소(`pointerEvents` 없음, 클릭/드래그에 관여하지 않음)
- 1차 시도(트랙·핸들 전체 커스텀 스타일링 + CSS `transition`)는 네이티브 채움 효과 손실 및 시각적 완성도 미흡으로 **폐기**, 네이티브 슬라이더 유지 + 눈금만 별도 오버레이하는 현재 방식으로 확정

##### (4) "신규유입 변화" 섹션 — 즉시효과 + +3M 잔액

| 라벨 | 단위 | 산출 | 비고 |
|---|---|---|---|
| 신규취급액 (주) | 억 | `newWkOld → newWkNew` (소수점 1자리) | 당주 즉시효과 |
| 순증액 (주) | 억 | `netWkOld → netWkNew` (소수점 1자리) | 당주 즉시효과 |
| **+3M 예상 대출잔액 (총합)** | 억 | `(balOld+165.2) → (balNew+165.2)` (소수점 1자리) | `balOld/balNew`는 "현재 잔액 + 주간 순증액×12주" 누적 방식. 전략 1~7등급 + 레거시 8~10등급 구분 2줄 표기 |
| 대출 건수 (주간) | 건 | `cntOld → cntNew` (정수, 콤마) | 당주 즉시효과 |
| **예상 신규고객 유입(주간)** | 명 | `custGainOld → custGain` (정수, 콤마) | 당주 즉시효과. 하단에 "3개월 평균 당행 최초 등록고객 비율 반영" 표시 |

- `custGainOld = Σ(cntOld × newCustRatio/100)` / `custGain = Σ(cntNew × newCustRatio/100)` (절대 수치, before/after 비교)
- **v2.2 변경 — delta 표시 위치**: "+3M 예상 대출잔액 (총합)" 카드의 변화량(▲/▼ `+n억`)이 기존엔 "전략 1~7등급" 보조줄에만 표시되어 헤드라인 합계와 분리된 것처럼 오인될 소지가 있었음 → 다른 카드들과 동일하게 **헤드라인 숫자 바로 아래**로 이동(`renderCard`에서 `deltaBlock`을 변수로 추출해 legacy/non-legacy 양쪽에서 공통 사용). 값 자체는 변경 없음(레거시 8~10등급이 고정값이라 delta는 동일).
- 미니노트에 "대출 잔액(총합)은 +3개월 예상치, 그 외 카드는 당주 즉시효과" 구분 안내 유지

##### (5) "추정 손익" 섹션 — +3개월 후 예상(월간 기준)

| 라벨 | 단위 | 산출 |
|---|---|---|
| 이자수익 (월) | 백만원 | `toMonthlyMM(intOld) → toMonthlyMM(intNew)` |
| 공헌이익 (월) | 백만원 | `toMonthlyMM(cmOld) → toMonthlyMM(cmNew)` |
| 영업이익 (월) | 백만원 | `toMonthlyMM(opOld) → toMonthlyMM(opNew)` |

- 부제: "(+3개월 후 예상 — 월간 기준)"
- 미니노트: "추정손익은 주간 순증액이 12주(약 3개월)간 유지되어 잔액이 그만큼 변화한다고 가정했을 때, 그 시점(+3개월 후)의 월간 손익 추정치입니다"

##### (6) 등급별 상세 결과 테이블
- 1~7등급, 가로 스크롤
- 컬럼: 등급 / 조정(bp) / 이탈율(%) / 건수(주) / 취급액(주,억) / 순증액(주,억) / **+3M 잔액(억)** / **+3M 이자수익(억)** / **+3M 공헌이익(억)** / **+3M 영업이익(억)**

##### (7) 요약 차트 ① — 주간 물량
- BarChart, 3개 카테고리: 신규실행액 / 대출건수 / 순증액 (1~7등급 기준, 당주 즉시효과)
- 막대 2계열: `curr`("현재", barInactive) / `adj`("조정 후", primary)
- `barSize=40`, `barCategoryGap="20%"`, 높이 280px, 막대 상단 LabelList

##### (8) 요약 차트 ② — +3M 월간 수익성
- 차트 제목에 "+3M" 표기
- BarChart, 2개 카테고리: 이자수익 / 공헌이익 (백만원/월)
- 막대 2계열: `curr` / `adj`, 높이 280px

##### (9) 간접효과 섹션
- **gainBanner**: "조정 후 주간 신규고객 **{N}명** 기준" (`custGain` 절대 수치, primary 색)
- IndirectCard 3종:

| 카드 | 라벨 | 입력 라벨 | 분모 | 산출 |
|---|---|---|---|---|
| 카드발급고객 증가 | "신규 카드발급 고객 증가 (주간)" | 유입 고객 × n% | `custGain` (절대) | `custGain × (cardRate/100)` 명, 기본 cardRate=35% |
| 수신평잔 증가 | "수신평잔 증가효과 (주간)" | 사장님 신용대출 고객 1개월 평잔 | `custGain` (절대) | `custGain × depositRate / 100` 백만원, 기본 depositRate=338만원(실측) |
| MAU 고객 증가 | "MAU 고객증가 (주간)" | 유입 고객 × n% | `custGain` (절대) | `custGain × (mauRate/100)` 명, **기본 mauRate=260%** |

- 미니노트: "**\* n 값은 사장님 대출 보유고객의 3개월 평균 실측 값이 입력되어 있으며, 수기 조정 가능함.**"

---

### 3.2 탭 2 — 성과 모니터링 (`ResultPage`) _(v2.3 변경: 탭명 "시행 실적" → "성과 모니터링")_

#### 3.2.1 목적
과거 3건의 금리 변경 이벤트(3/31, 4/23, 5/29)에 대해 전/후의 주간 영업지표, 월간 수익성, 퍼널 전환율 변화를 CSV 기준으로 확인.

> **수익성 지표(공헌이익/영업이익)는 simbase 원가율과 무관합니다.** `template_3_monthly_*.csv`에 입력된 실측값(원천 시스템 산출 완료값)을 그대로 집계·표시합니다.

#### 3.2.2 이벤트 정의 _(v2.3 변경: ev529 라벨 수정)_
| ID | 라벨 | 기준 주(refWeek) | 전(before) 월 | 후(after) 월 |
|---|---|---|---|---|
| `ev331` | 3/31 본부조정금리변경 | 4월 1주 | 1월·2월·3월 | 4월 |
| `ev423` | 4/23 본부조정금리변경 | 5월 1주 | 3월·4월 | 5월 |
| `ev529` | **5/29 토스 우대금리 도입** | 6월 1주 | 4월·5월 | 6월 |

> `ev529`의 주간 실적(`template_1_weekly_ev529.csv`) `after` 구간이 기존 3주(6월1~3주)에서 **6월4주를 추가해 4주로 확장**됨(실데이터 반영, 2026-06-26 이후). before/after 평균 비교가 6월4주를 포함해 재계산됨.

#### 3.2.3 이벤트별 금리 조정 내역 (`BP_CHANGES`) _(v2.3 신규)_

탭 선택 시 해당 이벤트의 등급별 bp 조정 내역을 칩(chip) 형태로 표시. 역사적 확정값이므로 **하드코딩**으로 관리.

| 이벤트 | 조정 내역 칩 |
|---|---|
| ev331 | 1~4등급 −6bp |
| ev423 | 1등급 −5bp / 2등급 −8bp / 3등급 −44bp / 4등급 −39bp |
| ev529 | 토스 플랫폼 유입고객 전 등급 −10bp |

> **ev529 방법론 주의사항**: 실제로는 토스 채널 유입 고객(전체의 약 30%)에게만 −10bp 할인이 적용되었으나, 탄력성 분석 시에는 포트폴리오 가중평균인 −3bp를 투입값으로 사용함(`template_5_elasticity.csv` 참조). 칩에는 실제 적용 할인폭(−10bp)을 표기하되, 탄력성 계수 추정치는 이질적 처치(Heterogeneous Treatment) 특성상 비토스 채널 대비 민감도를 과소추정할 수 있으므로 보수적으로 해석 필요.

#### 3.2.4 화면 구성 (상→하)

##### (1) 안내 배너: 이벤트별 탭 선택 → 해당 변경 전/후 지표 표시, 수익성은 "익월 중순 집계" 기준

##### (2) 이벤트 탭 선택 (3개)

##### (3) 등급별 bp 조정 내역 칩 _(v2.3 신규)_
탭 전환 시 `BP_CHANGES[activeEvent]` 기준으로 칩 동적 렌더링. 칩 스타일: primary 계열 배경/테두리, bp 수치는 `down(#dc2626)` 색으로 강조.

##### (4) 이벤트별 업로드: 주간 실적 CSV / 퍼널 CSV

##### (5) 수익성 지표 섹션 안분 추정 안내 배너 _(v2.3 신규)_
```
⚠ 현재 고신용/중저신용 수익성 지표는 고객 비율 기반 안분 추정치입니다.
   실데이터 구축 및 반영 예정: 2026년 8월말
```
노란 계열(amber) 배경 · 테두리. 고신용/중저신용 수익성 카드 그리드 상단에 배치.

> **안분 배경**: 고신용/중저신용 분리 실데이터는 2026년 8월말 구축 예정. 현재는 전체 수익성 지표를 고신용/중저신용 고객 수 비율(안분)로 배분하여 산출한 추정치를 사용 중.

##### (6) 주간 영업 지표 KPI (5종)
`newLoan` / `netLoan` / `balance` / `count` / `newCust` — before 평균 대비 after 평균 delta%

##### (7) 주간 차트: 신규실행액·순증액 추이, ReferenceLine(금리변경일)
> Y축(억) 3개 이벤트 공통 고정(v1.2): `weeklyAxisMax = ceil(max(전체 이벤트 newLoan·netLoan) × 1.15 / 10) × 10` (§4.7)

##### (8) 수익성 지표
- (8-1) 고신용자: `template_3_monthly_high.csv` 기준, `interest`/`contribution`/`operatingProfit` × 100 (백만원). 섹션 배지 "익월 중순 집계"
- (8-2) 중저신용자: `template_3_monthly_low.csv` 기준
- (8-3) 통합 월간 추이 차트: 고신용+중저신용 합산, 1월~6월 막대 3계열

##### (9) 퍼널 전환율: 플랫폼→당행 유입률 / 가심사승인→실행 전환율 (등급별 before/after 바차트)

---

## 4. 계산 로직 정의

### 4.1 공통 유틸리티
| 함수 | 정의 |
|---|---|
| `avg(rows, key)` | 배열 평균 (빈 배열→0) |
| `sumBy(rows, key)` | 배열 합계 |
| `toMonthlyMM(v)` | `v × 100 / 12` — 연간(억) → 월간(백만원) |

### 4.2 `computeRows(adjMap, base, cofVal)` — 등급별 시뮬레이션

> **부호 규칙**: `legalCostRate`, `contractCreditRate`, `contractOpRate`, `creditGapRate`, `opGapRate` 5종은 CSV에서 음수(−) 입력. `nonIntRate`·`newCustRatio`는 양수(+) 입력.

```
churnNew    = max(0, churn + (bp/10) × elasticity)
retainRatio = (100 − churnNew) / (100 − churn)
netWkNew    = netWk × retainRatio

// +3개월(12주) 예상 잔액 — 주간 순증액 누적 방식
balOld      = balance + netWk    × 12   // 조정 없이 현재 추세 유지 시
balNew      = balance + netWkNew × 12   // 조정 후 추세 반영 시

marginNew   = margin + bp/100
intOld      = balOld × margin   / 100                       // +3M 시점 연간 이자수익(억원)
intNew      = balNew × marginNew / 100

cmRateOld   = margin    − cof + legalCostRate + contractCreditRate
cmRateNew   = marginNew − cof + legalCostRate + contractCreditRate
cmOld/New   = balOld/balNew × cmRate / 100                   // +3M 시점 연간 공헌이익(억원)

opAdjRate   = −contractOpRate − creditGapRate − opGapRate − nonIntRate
opOld/New   = cmOld/New − balOld/balNew × opAdjRate / 100    // +3M 시점 연간 영업이익(억원)

cntNew = count × retainRatio  /  newWkNew = newWk × retainRatio
newCustRatio: 해당 등급 실측 신규고객 비율 — computeRows 반환값에 포함
```

### 4.3 `runSimulation(adjMap, base, cofVal, indirectParams)`

```
custGainOld = Σ (r.cntOld × r.newCustRatio / 100)
custGain    = Σ (r.cntNew × r.newCustRatio / 100)

cardEffect    = custGain × (cardRate / 100)
depositEffect = custGain × depositRate / 10000
mauEffect     = custGain × (mauRate / 100)

반환: { rows, tot, indirect: { custGainOld, custGain, cardEffect, depositEffect, mauEffect } }
```

### 4.4 `findOptimalAdj(base, cofVal, { churnCap=15, range=50, step=5, objective })` — "균형성장" 포트폴리오 최적화 _(v2.2 전면 재설계)_

> 기존(v2.1까지)에는 등급별로 **독립적으로** 그리드서치하며 "해당 등급 자체의 잔액·공헌이익이 둘 다 양수/성장"인 bp를 찾았으나, 실데이터로 돌려보니 1~4등급 모두 마진율이 1.13~2.7%로 얇아 **등급 단독으로는 두 지표가 동시에 +가 되는 bp가 존재하지 않음**(어느 방향이든 한쪽은 늘고 한쪽은 줆)이 확인됨. 사용자가 "등급별로는 트레이드오프해도 **1~7등급 합계**가 둘 다 +면 된다"는 포트폴리오 관점을 제시 → 알고리즘을 다음과 같이 전면 재설계.

**문제 정의**: 등급별 bp 후보(−50~+50, 5bp 간격, `churnCap` 제약 적용)를 7개 등급에 대해 동시에 선택하여,
- **공헌이익 위주**: `Σ(balNew−balOld) ≥ 0` 제약 하에 `Σ(cmNew−cmOld)` 최대화
- **자산성장 위주**: `Σ(cmNew−cmOld) ≥ 0` 제약 하에 `Σ(balNew−balOld)` 최대화

등급×bp 후보 조합이 21^7(약 18억) 가지라 전체탐색은 불가능 → **동적계획법(DP, multi-choice knapsack 구조)**으로 정확한 최적해를 산출:

```
// 등급별로 bp 후보 각각의 (dBal, dCm) 계산
gradeCandidates = base.map(s => { bp별 dBal, dCm 산출 (computeRows와 동일 로직) })

// dp: bin(이산화 키, 상태공간 축약용) → { score: 목적함수 누적값, exact: 제약변수 정밀 누적값, picks }
dp = { 0: { score:0, exact:0, picks:{} } }
gradeCandidates.forEach(grade별로 dp 갱신: 각 bp 후보를 적용한 새 상태로 전이, 같은 bin 내에서는 score가 더 높은 것만 유지)

// 최종: exact(정밀 누적값) ≥ 0인 상태들 중 score(목적함수)가 최대인 조합 채택
best = dp 중 exact≥0 이고 score 최대인 상태
return best.picks   // { grade: bp, ... } 형태, 7개 등급 전부 포함
```

- **정밀도 버그 수정**: 최초 구현 시 이산화 bin(0.1억 단위) 값을 역산해 누적치를 계산했더니, 등급을 거칠 때마다 반올림 오차가 누적되어 경계 부근 해가 미세하게(예: −0.07억) 제약을 위반하는 문제가 있었음 → DP state에 **정밀 누적값(`exact`)을 별도로 들고 가서** bin은 상태공간 축약(중복 제거) 용도로만 쓰고, 최종 판정은 `exact` 기준으로 하도록 수정.
- **실데이터 검증 결과** (1~7등급, cof=2.8%): 공헌이익 위주(1등급−25bp, 2~7등급+50bp 등)→포트폴리오 합계 잔액 **+9.1억** / 공헌이익 **+64.5억**. 자산성장 위주(1등급−50bp, 2등급−15bp, 5등급−30bp, 나머지 +50bp 등)→잔액 **+169.4억** / 공헌이익 **+0.07억**.
- **버튼 동작은 100% 브라우저 클라이언트 사이드**에서 실시간 계산되므로, 실데이터 CSV를 업로드한 뒤 버튼만 누르면 바로 그 데이터 기준 최적해가 나옴(별도 분석 요청 불필요).

### 4.5 ResultPage 집계
- 주간 KPI: `avg(before/after, key)` → delta%
- 월간 KPI: `avg(filtered by beforeMonths/afterMonths, key) × 100` (억→백만원)
- 통합 차트: 고신용 + 중저신용 합산 × 100

### 4.6 `LEGACY_BALANCE` 상수
```js
const LEGACY_BALANCE = 165.2; // 8~10등급 레거시 잔액(억) — 조정 범위 외
```
- 적용: Simulator "+3M 예상 대출잔액 (총합)" 카드 단 한 곳
- 시뮬레이션 반영 안 됨 (취급액/수익성 계산은 1~7등급만)
- 변경 시 HTML 내 상수 직접 수정

### 4.7 `weeklyAxisMax` — 주간 차트 공통 Y축 상한 (ResultPage)
```
vals          = 모든 이벤트(weeklyByEvent: ev331+ev423+ev529)의 newLoan, netLoan 값 전체
weeklyAxisMax = ceil( max(vals) × 1.15 / 10 ) × 10
```
적용 위치: ResultPage 주간 차트(§3.2.4 (7))의 `<YAxis domain={[0, weeklyAxisMax]} />`.

### 4.8 `elasticity`(탄력성 계수) 추정 방법론 — 요약

**모형** (등급별 개별 OLS 회귀, 1~4등급만): `churn_i,t = β0 + β1·bp_i,t + β2·ret_t + ε_i,t`, `elasticity = β1 × 10`.

**최종 결과** (2026-06-30, 6월4주 확정치 반영):

| 등급 | elasticity (10bp당) | R² |
|---|---|---|
| 1등급 | 1.65 | 0.15 |
| 2등급 | 0.93 | 0.08 |
| 3등급 | 0.36 | 0.04 |
| 4등급 | 0.74 | 0.11 |

> 6월4주 데이터가 잠정치(~6/25 기준)에서 확정치로 교체되며 기존 추정치(2.15/1.44/0.50/0.91)보다 소폭 하향 조정됨. 방향(부호)은 그대로 유지, 다중공선성도 여전히 거의 없음(상관계수 −0.04). 상세 진단 과정은 [`CSV_데이터_가이드.md`](./CSV_데이터_가이드.md) 부록 참고.

---

## 5. CSV 데이터 입력 명세

> 컬럼별 상세 정의: [`CSV_데이터_가이드.md`](./CSV_데이터_가이드.md)

> **비용율 부호 규칙**: `legalCostRate`, `contractCreditRate`, `contractOpRate`, `creditGapRate`, `opGapRate` → **음수(−)** 입력. `nonIntRate`, `newCustRatio` → **양수(+)** 입력.

| CSV 파일 | 업로드 위치 | 적용 범위 | 파서 |
|---|---|---|---|
| `template_2_simbase.csv` | 시뮬레이터 탭 "기준값 CSV" | 1~7등급 시뮬레이션 기준값 | `parseSimBase` |
| `template_1_weekly_ev331/423/529.csv` | 성과 모니터링 탭 이벤트별 "주간 실적 CSV" | 해당 이벤트 주간 영업지표 (3개 이벤트 합산값이 §4.7 Y축 상한 산출에도 사용됨) | `parseWeekly` |
| `template_3_monthly_high.csv` | 헤더 "고신용 수익성 CSV" | 고신용 1~6월 (실측 완제 수치) | `parseMonthly` |
| `template_3_monthly_low.csv` | 헤더 "중저신용 수익성 CSV" | 중저신용 1~6월 (실측 완제 수치) | `parseMonthly` |
| `template_4_funnel_ev331/423/529.csv` | 성과 모니터링 탭 이벤트별 "퍼널 CSV" | 해당 이벤트 퍼널 전환율 | `parseFunnelWide` |

**`template_2_simbase.csv` 컬럼 현황**:
| 컬럼 | 데이터 상태 |
|---|---|
| 원가율 6종 (`legalCostRate` 등) | ✅ 실측값 |
| `newCustRatio` (당행 최초 등록고객 비율) | ✅ 실측값 (3개월 평균) |
| `elasticity` (금리 탄력성 계수) | ✅ 1~4등급 실측값(§4.8, 6월4주 확정치 반영) / ⚠️ 5~7등급은 보류(표본 부족) |
| `margin`, `balance`, `count` 등 영업지표 | ✅ 실측값 |

**분석용 CSV (대시보드 비연동, elasticity 추정 전용)**:
| 파일 | 용도 |
|---|---|
| `template_5_elasticity.csv` | 등급×4구간 이탈율 + 이벤트별 bpChange — 1차 단순 전/후 비교용 (ev529는 가중평균 −3bp 투입) |
| `template_6_weekly_churn.csv` | 등급(1~4)×주간(26주) 이탈율 — 회귀분석 종속변수 |
| `template_6_stock_index.csv` | 주간 주가지수 — 회귀분석 통제변수 원천 |

---

## 6. 비기능 요구사항

| 구분 | 내용 |
|---|---|
| 배포 형태 | 단일 HTML 파일 — 서버/빌드 없이 브라우저 직접 실행 |
| 파일 크기 | **약 90KB** |
| 데이터 영속성 | 없음 — 새로고침 시 데모 데이터로 초기화 |
| 데모 데이터 | `DEMO_WEEKLY`, `DEMO_MONTHLY_HIGH/LOW`, `DEMO_SIM_BASE`, `DEMO_FUNNEL` 내장 |
| CSV 인코딩 | UTF-8 BOM / UTF-16 LE BOM / EUC-KR 자동 감지 |
| 네트워크 의존성 | esm.sh CDN 인터넷 접근 필요. 행내 PC + 행내 와이파이 환경에서 정상 작동. 오프라인 불가. |
| 반응형 | `repeat(auto-fit, minmax(...))` 기반 그리드 |
| 언어/단위 | 한국어 전용. 억원(물량) / 백만원(월간 수익성·수신평잔) |

---

## 7. 미결정 / 추후 협의 항목 (Open Items)

1. **5~7등급 elasticity 추정 보류**: 표본 부족(이벤트별 bp=0 다수). 향후 금리 조정 사례 추가 누적 시 재추정.
2. **+3M(12주) 예상 잔액 산식 검증**: "조정 후 순증액이 12주간 그대로 유지된다"는 선형 추정 가정이 실데이터로도 합리적인지(특히 급성장/급변동 등급에서 과대추정 가능성) 지속 검토 필요.
3. **자금원가율(cof) 자동 연동**: 현재 직접 입력(기본값 2.8%). 자동화 방식 전환 검토.
4. **간접효과 전환율(카드발급 n%) 검증**: 현재 가정값(35%). 수신평잔(338만원)·MAU(260%)는 실측/조정 완료.
5. **고신용/중저신용 분류 기준 문서화**: 신용평가 등급 컷오프 기준 명확화 필요.
6. **이벤트(EVENTS) 확장성**: 신규 금리 변경 이벤트 추가 시 `EVENTS` 배열 + `BP_CHANGES` + CSV 3종 + `beforeMonths`/`afterMonths` 동시 갱신 필요.
7. **"수신평잔 증가효과" 단위 적절성**: 백만원 표시 자릿수 실데이터 기준 재검토.
8. **`LEGACY_BALANCE` 갱신 프로세스**: 하드코딩(165.2억). 변동 시 HTML 직접 수정 필요.
9. **오프라인 대응**: CDN 의존 구조상 오프라인 불가. 중요 보고 전날 PDF 백업 권장.
10. **주간 차트 Y축 헤드룸 비율(1.15) 검증**: 실데이터 업로드 후 라벨 잘림/여백 과다 여부 육안 확인 필요.
11. **슬라이더 눈금 정렬 정밀도** _(v2.2 신규)_: 눈금은 네이티브 슬라이더 트랙 양끝에서 약 7px 인셋을 가정해 위치를 잡았는데, 브라우저별 기본 thumb 크기 차이로 미세한 오차가 있을 수 있음. 육안상 크게 거슬리지 않으면 그대로 유지.
12. **고신용/중저신용 수익성 실데이터 반영** _(v2.3 신규)_: 현재 고객 비율 안분 추정치 사용 중. 2026년 8월말 실데이터 구축 후 `template_3_monthly_*.csv` 교체 필요.
13. **ev529 탄력성 계수 해석 주의** _(v2.3 신규)_: 토스 채널(30%) 대상 −10bp 이벤트를 포트폴리오 가중평균 −3bp로 투입해 추정한 계수는 이질적 처치(Heterogeneous Treatment) 특성상 실제 민감도를 과소추정할 가능성 있음. 8월 실데이터 구축 시 토스/비토스 세그먼트 분리 분석 병행 권장.

---

## 8. 변경 이력 (요약)

| 일자 | 버전 | 변경 내용 |
|---|---|---|
| 2026-06-12 ~ 2026-06-20 | v0.1~v1.1 | (이전 버전 이력은 `요구사항정의서_v2.1.md` §8 참고) |
| 2026-06-24 | v1.2 | 시행 실적 탭 주간 차트 Y축 3개 이벤트 공통 고정(`weeklyAxisMax`) |
| 2026-06-25 | v2.0 | 탄력성 계수(elasticity) 1~4등급 실측 추정 완료(다중회귀분석), 가정값 안내박스/푸터 정리, 간접효과 카드 명칭·기본값 실측화 |
| 2026-06-26 | v2.1 | 대출잔액 산정방식을 "이탈율 비율 스케일링"→"주간 순증액×12주 누적(+3M)"으로 전환(손익까지 cascade), 간접효과 카드 라벨에 "(주간)" 추가 + `mauRate` 60→70%, "익월 4일"→"익월 중순" |
| 2026-06-26 (추가) | — | 잔액 카드 delta 위치를 헤드라인 숫자 아래로 이동. "최적금리 찾기"→"균형성장" 버튼 알고리즘 3단계 진화(절대값체크→등급별성장체크→**포트폴리오 DP 최적화**, §4.4). 6월4주 elasticity 확정치 반영(1.65/0.93/0.36/0.74로 하향), ev529 주간 실적 after 구간 3주→4주 확장. |
| 2026-06-30 | v2.2 | ① 금리 조정 슬라이더에 **5bp/10bp 차등 눈금** 추가(네이티브 채움 유지, 별도 오버레이) ② `mauRate` 기본값 70%→**260%** ③ 잔액 카드 delta 위치 수정, 균형성장 버튼 포트폴리오 DP 재설계, elasticity 확정치 반영을 문서에 정식 반영 ④ `standalone(backup0630).html` 백업 생성 |
| 2026-07-02 | **v2.3** | ① 탭 버튼 **"시행 실적" → "성과 모니터링"** ② 수익성 지표 섹션 상단 **안분 추정 안내 배너** 추가 (amber 계열, 실데이터 반영 예정 2026년 8월말 명시) ③ 성과 모니터링 탭 — 이벤트별 **금리 조정 내역 칩** 추가 (`BP_CHANGES` 하드코딩: ev331 1~4등급 −6bp / ev423 등급별 −5~−44bp / ev529 토스 플랫폼 유입고객 전 등급 −10bp) ④ ev529 이벤트 라벨 **"토스/현카 우대금리 도입" → "토스 우대금리 도입"** |
